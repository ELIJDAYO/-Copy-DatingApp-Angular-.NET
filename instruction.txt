Section 2
    dotnet --info
    dotnet new list
    dotnet new sln
        dotnet sln -h
        dotnet sln list
    dotnet new webapi -n API
    dotnet sln add API

    install CS extension
    
    cd API dotnet run (controller listening to an end point)

    dotnet run -lp https
    (need ssl cert?)

    after editing some settings.json
    dotnet run

    after removing ItemGroup from api.csproj
    dotnet restore

    installed nuget gallery extension
    crtl+p, >microsoft.entityFrameWork, install sqlite to API.csproj